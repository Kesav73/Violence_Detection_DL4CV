# Core scientific stack
numpy>=1.24,<3.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
scikit-learn>=1.3,<2.0
tqdm>=4.66,<5.0

# Computer vision
opencv-python>=4.8,<5.0

# PyTorch stack
# Note:
# - `pip install -r requirements.txt` is fine for CPU usage.
# - For GPU training/inference, install the matching CUDA build of
#   `torch` and `torchvision` from the official PyTorch instructions
#   if your local machine needs a specific CUDA version.
torch>=2.2,<3.0
torchvision>=0.17,<1.0

# Dataset access for the local training notebook
kagglehub>=0.3,<1.0

# Notebook support
jupyterlab>=4.0,<5.0
ipykernel>=6.29,<7.0
